{
  "name": "alphanest-subgraph",
  "version": "0.1.0",
  "description": "The Graph subgraph for AlphaNest AlphaGuard protocol",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ alphanest-subgraph",
    "create-local": "graph create --node http://localhost:8020/ alphanest-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ alphanest-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 alphanest-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.60.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
