# 积分系统设计 - 基于 PopCowDefi 代币

## 📋 概述

积分系统完全使用 **PopCowDefi** 代币，无需单独的积分代币。用户通过完成任务、交易、推荐等行为获得 PopCowDefi，然后可以消耗 PopCowDefi 购买平台权益。

---

## 🎯 核心设计理念

### 1. 统一代币模型

- ✅ **积分 = PopCowDefi**: 所有积分操作都使用 PopCowDefi 代币
- ✅ **直接价值**: 积分有直接代币价值，无需兑换
- ✅ **简化系统**: 减少代币种类，降低复杂度

### 2. 积分获取方式

| 行为 | 奖励（PopCowDefi） | 说明 |
|------|-------------------|------|
| **验证持仓** | 100 PopCowDefi | 验证多链持仓（24小时冷却） |
| **完成任务** | 50 PopCowDefi | 完成社区任务 |
| **交易** | 10-30 PopCowDefi | 根据交易金额（$1k: 10, $10k: 30） |
| **推荐用户** | 200 PopCowDefi | 成功推荐新用户 |
| **销毁尸体币** | 50 + 价值10% | 基础 50 + 按价值 10% 转换 |

### 3. 积分消耗方式

| 权益 | 消耗（PopCowDefi） | 说明 |
|------|------------------|------|
| **保险入场券** | 1,000 PopCowDefi | 购买 CowGuard 保险 |
| **抽奖券** | 500 PopCowDefi | 参与新项目抽奖 |
| **Alpha 信号** | 2,000 PopCowDefi | 购买高级 Alpha 信号 |

---

## 💰 积分规则详解

### 1. 验证持仓 (Verify-to-Earn)

```rust
// 验证多链持仓获取积分
pub fn verify_holding_and_earn(
    chain_id: u16,        // Solana/Base/BNB Chain
    token_address: Pubkey,
    min_balance: u64,
) -> Result<()>

// 奖励: 100 PopCowDefi
// 冷却: 24小时
```

**使用场景**:
- 用户连接多链钱包
- 验证在其他链上的持仓
- 获得 PopCowDefi 奖励

### 2. 完成任务 (Task-to-Earn)

```rust
// 完成任务获取积分
pub fn complete_task_and_earn(
    task_id: u32,
) -> Result<()>

// 奖励: 50 PopCowDefi
// 限制: 每个任务只能完成一次
```

**任务类型**:
- 关注 Twitter
- 加入 Discord
- 分享链接
- 邀请好友
- 质押代币

### 3. 交易奖励 (Trade-to-Earn)

```rust
// 交易获取积分
pub fn trade_and_earn(
    trade_amount_usd: u64,
) -> Result<()>

// 奖励计算:
// - $1,000 以下: 10 PopCowDefi
// - $1,000 - $10,000: 20 PopCowDefi
// - $10,000 以上: 30 PopCowDefi
```

**激励机制**:
- 鼓励用户交易
- 增加平台交易量
- 根据交易金额差异化奖励

### 4. 推荐奖励 (Referral-to-Earn)

```rust
// 推荐用户获取积分
pub fn referral_and_earn(
    referee: Pubkey,
) -> Result<()>

// 奖励: 200 PopCowDefi
// 限制: 每个用户只能推荐一次
```

**推荐机制**:
- 用户分享推荐链接
- 新用户通过链接注册
- 推荐人获得奖励

### 5. 尸体币销毁 (Burn-to-Earn)

```rust
// 销毁尸体币获取积分
pub fn burn_dead_coin_and_earn(
    dead_coin_amount: u64,
    dead_coin_value_usd: u64,
) -> Result<()>

// 奖励: 50 PopCowDefi + 价值 10% 的 PopCowDefi
```

**复活机制**:
- 用户销毁归零代币
- 按价值 10% 转换为 PopCowDefi
- 基础奖励 50 PopCowDefi

---

## 🎁 积分消耗场景

### 1. 保险入场券

```rust
// 消耗积分购买保险入场券
pub fn consume_points_for_insurance() -> Result<()>

// 消耗: 1,000 PopCowDefi
// 权益: 可以购买 CowGuard 保险产品
```

**使用场景**:
- 用户想购买 Rug Pull 保险
- 需要先消耗 PopCowDefi 获得入场券
- 然后才能购买保险

### 2. 抽奖券

```rust
// 消耗积分购买抽奖券
pub fn consume_points_for_raffle(
    ticket_count: u8,
) -> Result<()>

// 消耗: 500 PopCowDefi / 张
// 权益: 参与新项目空投抽奖
```

**使用场景**:
- 新项目上线前抽奖
- 用户消耗 PopCowDefi 购买抽奖券
- 中奖用户获得新项目代币

### 3. Alpha 信号

```rust
// 消耗积分购买 Alpha 信号
pub fn consume_points_for_alpha_signal() -> Result<()>

// 消耗: 2,000 PopCowDefi
// 权益: 获得高级 Alpha 交易信号
```

**使用场景**:
- 用户想获得早期 Alpha 信号
- 消耗 PopCowDefi 购买信号
- 获得高质量交易机会

---

## 📊 积分账户结构

### PointsAccount

```rust
pub struct PointsAccount {
    pub owner: Pubkey,
    pub total_points_earned: u64,    // 总获得积分
    pub total_points_consumed: u64,   // 总消耗积分
    pub available_points: u64,       // 可用积分（PopCowDefi 余额）
    
    // 统计
    pub verify_count: u32,
    pub task_count: u32,
    pub trade_count: u32,
    pub referral_count: u32,
    pub burn_count: u32,
    
    // 消耗统计
    pub insurance_tickets: u32,
    pub raffle_tickets: u32,
    pub alpha_signals: u32,
    
    // 其他数据
    pub last_verify_time: i64,
    pub total_trade_volume: u64,
    pub total_burned_value: u64,
    pub completed_tasks: Vec<u32>,
    pub referred_users: Vec<Pubkey>,
}
```

---

## 🔄 工作流程

### 积分获取流程

```
用户执行行为（验证/任务/交易/推荐/销毁）
    ↓
系统验证行为有效性
    ↓
从系统金库转移 PopCowDefi 到用户
    ↓
更新积分账户统计
    ↓
记录积分历史
```

### 积分消耗流程

```
用户选择要购买的权益
    ↓
检查可用积分（PopCowDefi 余额）
    ↓
从用户转移 PopCowDefi 到系统金库
    ↓
发放权益（入场券/抽奖券/信号）
    ↓
更新积分账户统计
```

---

## 💡 设计优势

### 1. 简化系统

- ✅ **单一代币**: 不需要单独的积分代币
- ✅ **直接价值**: 积分就是 PopCowDefi，有直接价值
- ✅ **易于理解**: 用户更容易理解

### 2. 价值捕获

- ✅ **代币需求**: 增加 PopCowDefi 需求
- ✅ **价值支撑**: 积分消耗支撑代币价值
- ✅ **循环经济**: 获取 → 消耗 → 再获取

### 3. 灵活管理

- ✅ **动态调整**: 管理员可以调整积分规则
- ✅ **成本控制**: 可以控制积分发放成本
- ✅ **运营工具**: 灵活的用户激励工具

---

## 📈 积分经济模型

### 积分获取预算

假设每月发放：
- 验证持仓：10,000 用户 × 100 = 1,000,000 PopCowDefi
- 完成任务：5,000 用户 × 50 = 250,000 PopCowDefi
- 交易奖励：1,000 用户 × 20 = 20,000 PopCowDefi
- 推荐奖励：500 用户 × 200 = 100,000 PopCowDefi
- 销毁尸体币：200 用户 × 100 = 20,000 PopCowDefi

**总计**: 约 1,390,000 PopCowDefi/月

### 积分消耗预算

假设每月消耗：
- 保险入场券：500 张 × 1,000 = 500,000 PopCowDefi
- 抽奖券：1,000 张 × 500 = 500,000 PopCowDefi
- Alpha 信号：200 个 × 2,000 = 400,000 PopCowDefi

**总计**: 约 1,400,000 PopCowDefi/月

### 平衡机制

- ✅ **收支平衡**: 获取和消耗基本平衡
- ✅ **动态调整**: 根据实际情况调整规则
- ✅ **储备资金**: 系统金库保持充足资金

---

## 🛠 管理员功能

### 1. 补充系统金库

```rust
pub fn refill_system_vault(
    amount: u64,
) -> Result<()>
```

**用途**: 当系统金库余额不足时，管理员补充 PopCowDefi

### 2. 更新积分规则

```rust
pub fn update_points_rules(
    points_per_verify: Option<u64>,
    points_per_task: Option<u64>,
    points_per_trade: Option<u64>,
    points_per_referral: Option<u64>,
    points_per_burn: Option<u64>,
    insurance_ticket_cost: Option<u64>,
    raffle_ticket_cost: Option<u64>,
    alpha_signal_cost: Option<u64>,
) -> Result<()>
```

**用途**: 根据运营情况动态调整积分规则

---

## 📋 实施检查清单

- [x] 积分系统合约开发
- [x] 积分获取功能（5种方式）
- [x] 积分消耗功能（3种权益）
- [x] 管理员功能
- [ ] 前端界面开发
- [ ] 任务系统集成
- [ ] 测试和审计

---

## 🎯 总结

### 核心特点

1. **统一代币**: 所有积分操作使用 PopCowDefi
2. **直接价值**: 积分有直接代币价值
3. **灵活管理**: 管理员可以动态调整规则
4. **循环经济**: 获取 → 消耗 → 再获取

### 业务价值

- ✅ **用户激励**: 鼓励用户参与平台活动
- ✅ **代币需求**: 增加 PopCowDefi 需求
- ✅ **价值支撑**: 积分消耗支撑代币价值
- ✅ **增长工具**: 推荐奖励促进用户增长

---

*最后更新: 2026年1月15日*  
*版本: 1.0*
