# 编译下一步行动指南

## 🎯 当前状态

**问题**: Anchor CLI 使用的 Cargo 版本 (1.84.0) 不支持 `edition2024` 特性

**影响**: 无法编译合约

**解决方案**: 已尝试降级 Anchor 到 0.29.0，但问题依然存在（依赖包需要新版本 Cargo）

---

## ✅ 推荐解决方案

### 方案 1: 使用 Docker（最简单）⭐

```bash
cd contracts/solana

# 使用 Solana 官方镜像
docker run --rm -v $(pwd):/workspace \
  -w /workspace \
  solanalabs/solana:latest \
  anchor build
```

**优点**:
- ✅ 无需修改系统
- ✅ 环境隔离
- ✅ 版本匹配

---

### 方案 2: 更新系统 Cargo

如果 Docker 不可用，需要更新系统 Cargo：

```bash
# 1. 更新 Rust 到最新版本
rustup update stable
rustup default stable

# 2. 检查 Cargo 版本（应该 >= 1.85.0）
cargo --version

# 3. 如果版本仍然旧，可能需要从源码编译
# 或者等待 Rust 工具链更新
```

---

## 📋 执行清单

### 立即执行
- [ ] 选择解决方案（推荐 Docker）
- [ ] 执行构建命令
- [ ] 验证构建成功

### 构建成功后
- [ ] 运行测试：`anchor test`
- [ ] 检查构建产物：`ls target/deploy/*.so`
- [ ] 准备部署到 Devnet

---

## 🔍 验证构建成功

构建成功后应该看到：
```
✅ Compiled successfully
✅ All programs built
✅ IDL generated
```

检查文件：
```bash
ls -la target/deploy/*.so
ls -la target/deploy/*-keypair.json
```

---

*最后更新: 2026年1月15日*
