# AlphaNest 智能合约业务逻辑文档

## 📋 目录

1. [合约架构概览](#合约架构概览)
2. [核心合约详解](#核心合约详解)
3. [业务流程](#业务流程)
4. [代币经济模型](#代币经济模型)
5. [安全机制](#安全机制)

---

## 合约架构概览

### 合约列表

| 合约名称 | 功能 | 状态 |
|---------|------|------|
| **AlphaToken** | 平台代币 ($ALPHA) | ✅ 核心 |
| **AlphaNestCore** | 积分系统、质押、分红 | ✅ 核心 |
| **AlphaGuard** | 参数化保险协议 | ✅ 核心 |
| **AlphaGuardOracle** | Rug Pull 判定预言机 | ✅ 核心 |
| **ReputationRegistry** | Dev 信誉评分系统 | ✅ 核心 |
| **TokenFactory** | Meme 代币发行工厂 | ✅ 核心 |
| **CrossChainVerifier** | 跨链持仓验证 | ✅ 核心 |

### 合约关系图

```
AlphaToken (ERC20)
    ↓
AlphaNestCore (质押、积分、分红)
    ↓
ReputationRegistry (Dev 信誉)
    ↓
TokenFactory (代币发行)
    ↓
AlphaGuard (保险)
    ↓
AlphaGuardOracle (判定)
```

---

## 核心合约详解

### 1. AlphaToken - 平台代币

**功能：**
- ERC-20 代币，支持燃烧、许可 (EIP-2612)
- 总供应量：10 亿 $ALPHA
- 支持暂停机制

**代币分配：**
```
社区激励: 40% (4亿) - 5年线性释放
生态发展: 20% (2亿) - 2年锁仓后3年释放
团队顾问: 15% (1.5亿) - 1年锁仓+3年释放
早期投资: 10% (1亿) - 6个月锁仓+2年释放
公开销售: 10% (1亿) - TGE 50%，6个月释放剩余
流动性:   5% (0.5亿) - TGE 全部释放
```

**关键函数：**
- `mintCommunityAllocation()` - 铸造社区激励
- `mintEcosystemAllocation()` - 铸造生态发展
- `burn()` - 燃烧代币（回购销毁）
- `initialDistribution()` - TGE 初始分发

---

### 2. AlphaNestCore - 核心业务逻辑

**功能模块：**

#### 2.1 质押系统
- **质押 $ALPHA**：最小 100 $ALPHA，获得分红和挖矿权重
- **解除质押**：7 天冷却期，防止闪电贷攻击
- **奖励分配**：协议收入 30% 分配给质押者
- **挖矿权重**：`权重 = 质押量 × 时间系数`（最高 2x）

**关键函数：**
```solidity
stake(uint256 amount)           // 质押
requestUnstake(uint256 amount)   // 请求解除质押
completeUnstake()                // 完成解除质押
claimRewards()                   // 领取奖励
getMiningWeight(address user)    // 获取挖矿权重
```

#### 2.2 积分系统
- **积分获取**：
  - 验证持仓：100 积分/次
  - 销毁尸体币：按价值 10% 转换
  - 管理员发放：批量发放
- **积分消耗**：
  - 兑换权益
  - 参与活动
  - 优先访问

**关键函数：**
```solidity
grantPoints(address user, uint256 amount, string reason)
verifyAndEarn(address user)
burnDeadCoin(address token, uint256 amount)
getPointsInfo(address user)
```

#### 2.3 费用分配
协议收入分配比例：
- **质押者**：30%
- **国库**：40%
- **回购销毁**：30%

**关键函数：**
```solidity
distributeRevenue(uint256 amount)  // 分配协议收入
```

#### 2.4 尸体币复活
- 用户销毁归零代币
- 按代币价值 10% 转换为积分
- 记录销毁历史

---

### 3. AlphaGuard - 参数化保险

**业务逻辑：**
1. **创建保险池**：针对特定代币创建对赌池
2. **购买保险**：用户选择立场（RUG/SAFE）并投注
3. **预言机判定**：到期后由预言机判定结果
4. **赔付分配**：胜者按比例分配资金池

**关键参数：**
- 最小池持续时间：1 小时
- 最大池持续时间：7 天
- 协议费率：1-10% (可配置)

**关键函数：**
```solidity
createPool(address token, uint256 duration, uint256 minBet, uint256 maxBet)
purchasePolicy(uint256 poolId, Position position, uint256 amount)
resolvePool(uint256 poolId, Outcome outcome)  // 仅预言机
claimPayout(uint256 policyId)
calculatePayout(uint256 policyId)
```

**赔付计算：**
```
赔付 = (投注金额 × 总资金池) / 获胜方总投注
```

---

### 4. AlphaGuardOracle - Rug 判定预言机

**判定标准：**
1. 流动性撤走 > 80%
2. 价格下跌 > 90% 在短时间内
3. Dev 钱包大量抛售
4. 合约被标记为恶意

**关键函数：**
```solidity
reportToken(address token, TokenStatus status, string evidence, ...)
autoDetectRug(address token, uint256 liquidityDrop, uint256 priceDrop, ...)
fileDispute(uint256 reportId, string reason)
finalizeAndNotify(uint256 reportId, uint256 poolId)
```

**争议机制：**
- 24 小时争议期
- 多签/DAO 最终裁决
- 支持争议和撤销

---

### 5. ReputationRegistry - Dev 信誉系统

**功能模块：**

#### 5.1 Dev 注册与评分
- **初始分数**：50 分
- **等级系统**：
  - UNRANKED: < 20
  - BRONZE: 20-39
  - SILVER: 40-59
  - GOLD: 60-79
  - PLATINUM: 80-94
  - DIAMOND: 95-100

#### 5.2 红V认证
- **申请条件**：分数 ≥ 60
- **质押要求**：10,000 $ALPHA
- **认证流程**：申请 → 审核 → 批准/拒绝

#### 5.3 发币追踪
- 记录 Dev 发币历史
- 追踪 ATH、市值、持有人数
- 标记 Rug 和毕业状态

#### 5.4 订阅系统
- 用户订阅 Dev
- 支持 Telegram/Discord 通知
- 自动跟单功能

**关键函数：**
```solidity
registerDev(address dev, string devAlias)
updateScore(address dev, uint256 newScore)
recordLaunch(address dev, address token, uint256 chainId, ...)
requestVerification(address dev)
approveVerification(address dev)
subscribe(address subscriber, address dev, ...)
```

---

### 6. TokenFactory - 代币发行工厂

**功能：**
- 标准化 Meme 代币创建
- 发行费用收取
- 代币追踪和索引
- Dev 关联记录

**发行配置：**
- 最小供应量：100 万
- 最大供应量：1 万亿
- 基础费用：0.01 ETH
- 认证 Dev 折扣：50%

**关键函数：**
```solidity
createToken(string name, string symbol, uint256 totalSupply)
createTokenFree(...)  // 仅 Operator，官方代币
verifyToken(address tokenAddress)
calculateFee(address creator)
```

---

### 7. CrossChainVerifier - 跨链验证

**功能：**
- 验证用户在其他链上的持仓
- 支持存储证明和签名验证
- 多链持仓聚合
- 验证结果缓存（1 小时有效期）

**支持的验证类型：**
1. **存储证明**：Herodotus/Axiom 风格
2. **签名验证**：Chainlink CCIP 风格
3. **预言机验证**：中心化预言机

**关键函数：**
```solidity
verifyHoldingBySignature(...)
submitVerification(...)  // 中继者提交
updateAggregatedHolding(address user)
hasValidHolding(address user, uint256 chainId, address token, uint256 minBalance)
```

---

## 业务流程

### 1. 用户质押流程

```
用户质押 $ALPHA
    ↓
记录质押信息（数量、时间）
    ↓
更新总质押量
    ↓
协议收入分配时计算奖励
    ↓
用户领取奖励
```

### 2. 积分获取流程

```
用户验证持仓（跨链）
    ↓
后端验证通过
    ↓
调用 verifyAndEarn()
    ↓
发放 100 积分
    ↓
记录积分历史
```

### 3. 保险购买流程

```
创建保险池（针对代币）
    ↓
用户选择立场（RUG/SAFE）
    ↓
支付保费进入资金池
    ↓
等待到期
    ↓
预言机判定结果
    ↓
胜者领取赔付
```

### 4. Dev 发币流程

```
Dev 注册（ReputationRegistry）
    ↓
申请红V认证（可选）
    ↓
通过 TokenFactory 发币
    ↓
记录发币历史
    ↓
追踪代币表现
    ↓
更新 Dev 评分
```

### 5. 跨链验证流程

```
用户在链 A 持有代币
    ↓
前端请求验证
    ↓
后端查询链 A 状态
    ↓
生成签名/存储证明
    ↓
提交到 CrossChainVerifier
    ↓
验证通过，发放积分
```

---

## 代币经济模型

### $ALPHA 代币用途

1. **质押分红**：质押获得协议收入 30% 分红
2. **挖矿权重**：质押量影响挖矿权重
3. **红V认证**：需要质押 10,000 $ALPHA
4. **回购销毁**：协议收入 30% 用于回购销毁

### 价值捕获

- **协议收入**：代币发行费、保险手续费
- **回购销毁**：通缩机制
- **质押锁仓**：减少流通量
- **生态激励**：社区激励分配

---

## 安全机制

### 1. 访问控制
- OpenZeppelin AccessControl
- 角色分离（Admin、Operator、Verifier）

### 2. 重入保护
- ReentrancyGuard 修饰符
- 所有外部调用前检查

### 3. 暂停机制
- Pausable 合约
- 紧急情况可暂停

### 4. 冷却期
- 解除质押：7 天冷却期
- 防止闪电贷攻击

### 5. 数值检查
- 最小/最大限制
- 溢出保护（Solidity 0.8+）

### 6. 预言机安全
- 多签控制
- 争议机制
- 24 小时争议期

---

## 合约交互示例

### 质押并领取奖励

```solidity
// 1. 质押
alphaNestCore.stake(1000 * 1e18);

// 2. 等待协议收入分配
// (由 Operator 调用 distributeRevenue)

// 3. 领取奖励
alphaNestCore.claimRewards();
```

### 购买保险

```solidity
// 1. 创建保险池（仅 Owner）
alphaGuard.createPool(
    tokenAddress,
    7 days,      // 持续时间
    10 * 1e6,    // 最小投注 (10 USDC)
    1000 * 1e6   // 最大投注 (1000 USDC)
);

// 2. 用户购买保险
alphaGuard.purchasePolicy(
    poolId,
    Position.RUG,  // 选择立场
    100 * 1e6      // 投注 100 USDC
);

// 3. 到期后预言机判定
alphaGuardOracle.finalizeAndNotify(reportId, poolId);

// 4. 用户领取赔付
alphaGuard.claimPayout(policyId);
```

### Dev 发币

```solidity
// 1. 注册 Dev
reputationRegistry.registerDev(devAddress, "DevAlias");

// 2. 申请认证（可选）
reputationRegistry.requestVerification(devAddress);

// 3. 发币
tokenFactory.createToken(
    "My Token",
    "MTK",
    1_000_000_000 * 1e18
);

// 4. 记录发币
reputationRegistry.recordLaunch(
    devAddress,
    tokenAddress,
    chainId,
    initialLiquidity
);
```

---

## 总结

AlphaNest 是一个完整的 Meme 代币生态系统，包含：

1. **代币经济**：$ALPHA 代币 + 质押分红
2. **积分系统**：激励用户参与
3. **保险机制**：保护用户免受 Rug Pull
4. **信誉系统**：追踪 Dev 表现
5. **代币发行**：标准化发币流程
6. **跨链验证**：支持多链持仓验证

所有合约都经过精心设计，确保安全性、可扩展性和用户体验。
